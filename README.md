# Rastgele Şifre Üretici

Bu uygulama, belirlenen karakter sayısına göre rastgele şifreler üretmek için tasarlanmıştır. Şifreler büyük ve küçük harfler, sayılar ve özel karakterler içerebilir.

## Özellikler

- Belirtilen uzunlukta rastgele şifre üretimi
- Şifre oluştururken harfler, sayılar ve özel karakterler kullanılır
- Basit ve anlaşılır bir konsol uygulamasıdır

## Programı Çalıştırma

Programı çalıştırmak için bilgisayarınızda Python 3.6 veya daha üst bir sürümün kurulu olduğundan emin olun.

## Testleri Çalıştırma

Burada yazılmış testler pytest kütüphanesini kullanır. Yüklemek için şu komutu yazın:
```bash
pip install pytest
```

Daha sonra testleri proje dizininin kök klasöründen şu komutla çalıştırabilirsiniz:
```bash
pytest
```
English:
# Random Password Generator

This application is designed to generate random passwords based on specified character criteria. The passwords incorporate uppercase and lowercase letters, numbers, and special characters.

## Features

- Generates random passwords based on specified criteria
- Uses letters, numbers, and special characters
- Simple and straightforward console application

## Running the Program

Ensure that Python 3.6 or a later version is installed to run the program.

## Running Tests

The tests included here use the `pytest` library. To install it, run:
```bash
pip install pytest
```

Then, you can run the tests from the project root directory using the following command:
```bash
pytest
```

Español:
# Generador de contraseñas aleatorias

Esta aplicación está diseñada para generar contraseñas aleatorias basadas en un número específico de caracteres. Las contraseñas pueden incluir letras mayúsculas y minúsculas, números y caracteres especiales.

## Características

- Genera contraseñas aleatorias de una longitud especificada
- Utiliza letras, números y caracteres especiales al crear las contraseñas
- Aplicación de consola sencilla y directa

## Ejecución del programa

Para ejecutar el programa, asegúrate de tener instalado Python 3.6 o una versión posterior en tu equipo.

## Ejecución de pruebas

Las pruebas incluidas aquí utilizan la biblioteca `pytest`. Para instalarla, ejecuta el siguiente comando:
```bash
pip install pytest
```

A continuación, puedes ejecutar las pruebas desde el directorio raíz del proyecto utilizando este comando:
```bash
pytest
```



Contributors:
ddagli1
Sena2303
Henfmss
AhmetTikir
Vantalation12
Alperxpro
Notxr
said1113
Ygz312629
asimgiray175
RpiPro2140

